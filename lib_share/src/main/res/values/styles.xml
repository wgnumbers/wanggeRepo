<resources>

    <!-- Base application theme. -->


    <style name="share_MyDialog" parent="@android:style/Theme.Holo.Light.Dialog.NoActionBar">
        <!--背景颜色及和透明程度-->
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="share_BottomSettingShareDialog" parent="share_MyDialog">
        <item name="android:windowAnimationStyle">@style/share_dialogAnim</item>
    </style>

    <style name="share_dialogAnim" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="@android:windowExitAnimation">@anim/dialog_exit</item>
    </style>


</resources>
